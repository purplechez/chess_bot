{
  "name": "embedded-browser",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"pnpm --filter client dev\" \"node server.js\"",
    "build": "pnpm --filter client build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}